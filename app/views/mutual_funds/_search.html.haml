= simple_form_for search, :url => mutual_funds_path, :method => :get, :html => {:novalidate => true}, :html => {:class => 'form-horizontal'} do |f|
  = f.label :name
  = f.text_field :name, data: { autocomplete_source: mutual_funds_path }
