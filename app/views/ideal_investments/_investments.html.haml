.span4
  %h5= t(".#{type}")
  %table.table.table-striped.table-bordered{ :data => { :role => type } }
    %tr
      %th Name
      %th
        = I18n.send('t', "ideal_investments.#{value}")
    -resource.send(type).each do |investment|
      %tr
        - if type == 'fixed_deposits'
          %td= investment.name
        - else
          %td= link_to investment.name, mutual_fund_path(resource.scheme(investment.name))
        %td= investment.send(value)