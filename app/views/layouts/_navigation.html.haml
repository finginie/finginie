%ul
  - %w(stocks portfolios).each do |resource|
    = link_to t(resource), :controller => "/#{resource}"
  = link_to t('financial_planner'), financial_planner_path
  = link_to t('investment_education'), page_path('investment_education')
  = link_to t('personal_financial_tools.home'), emi_calculators_path('emi_calculators')
