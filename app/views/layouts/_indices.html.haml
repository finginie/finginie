.row.show-grid
  %span{:data => {:role => 'market_indices'}}
    - %w(NiftyDecorator SensexDecorator GoldDecorator).each do |indices|
      .span4
        %h3.align-center
          = indices.constantize.name
          %small
            %strong= indices.constantize.last_traded_price
            =  rg_colorize indices.constantize.net_change
            (#{rg_colorize indices.constantize.percent_change} %)
