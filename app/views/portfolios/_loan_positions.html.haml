- unless loan_positions.empty?
  %section{ :class => "Loan" }
    %h1 Loans
    %table.table.table-striped
      %tr
        %th.name Name
        %th.center_align Borrow Date
        %th Interest
        %th Duration
        %th Outstanding Amt
        %th.center_align Clear Loan
      %tr
        %td.name
        %td
        %td (%p.a.)
        %td (years)
        %td (Rs)
        %td
      - loan_positions.each do |net_position|
        %tr
          %td.name= net_position.name
          %td.center_align= l net_position.date
          %td= net_position.rate_of_interest
          %td= net_position.period
          %td= net_position.outstanding_amount.abs
          %td.center_align= link_to 'Clear Loan', clear_portfolio_loan_transaction_path(net_position.first.portfolio_id, net_position.first), :method => :post
      %tr.total
        %td.name Total
        %td
        %td
        %td
        %td= loan_positions.sum(&:outstanding_amount).abs
        %td
