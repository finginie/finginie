= simple_form_for(@fixed_income) do |f|
  = f.error_notification

  .inputs
    = f.input :name
    = f.input :period
    = f.input :rate_of_interest
    = f.input :emi

  .actions
    = f.button :submit
