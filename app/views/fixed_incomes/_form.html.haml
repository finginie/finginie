= simple_form_for resource, :html => {:class => 'form-horizontal'} do |f|
  = f.error_notification

  .inputs
    = f.input :name
    = f.input :period
    = f.input :rate_of_interest
    = f.input :emi

  .actions
    = f.button :submit
