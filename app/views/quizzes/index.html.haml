%h1 Listing quizzes

%table
  %tr
    %th Type
    %th Name
    %th Weight
    %th
    %th
    %th

  - @quizzes.each do |quiz|
    %tr
      %td= quiz.type
      %td= quiz.name
      %td= quiz.weight
      %td= link_to 'Show', quiz
      %td= link_to 'Edit', edit_quiz_path(quiz)
      %td= link_to 'Destroy', quiz, :confirm => 'Are you sure?', :method => :delete

%br

= link_to 'New Quiz', new_quiz_path
