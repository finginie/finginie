= simple_form_for @search, :url => collection_path, :html => {:novalidate => true} do |f|
  = f.label :company_name, :required => false
  = f.text_field :company_name, data: { autocomplete_source: stocks_path }
